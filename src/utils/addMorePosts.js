// let countPosts = 12;

export function addMorePosts(posts) {
	function slicePosts() {
		const newPosts = posts.slice(0, 12);
		return newPosts;
	}

	const arr = slicePosts(countPosts);
	if (arr.length = 12){
		
	} return arr;
}
